/*
 * Arquivo de configuração privada para uso interno.
 * NÃO COMMITAR este arquivo em um repositório público.
 * Copie este arquivo para `local-config.js` e ajuste os valores.
 */

window.PRIVATE_CONFIG = {
  // URL do Power Automate para uso interno.
  urlUnified: '',

  // Hash SHA-256 da senha admin.
  // Configure este valor em um ambiente privado.
  adminPasswordHash: ''
};
